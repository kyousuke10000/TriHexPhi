# ワークフロー自動実行ガイド

**目的**: 手動実行（RUNボタン）を押さずに、自動実行されるワークフローの整理

---

## 📋 自動実行されるワークフロー

### 1. SeventhSense (Fusion)
- **ファイル**: `.github/workflows/seventhsense.yml`
- **自動実行**: ✅ 毎日00:00 UTC（日本時間09:00）
- **手動実行**: 任意（workflow_dispatch）
- **実行内容**: SeventhSense AIのProof生成

### 2. PR Ledger (Generate & Publish)
- **ファイル**: `.github/workflows/pr_ledger.yml`
- **自動実行**: ✅ 毎日00:00 UTC（日本時間09:00）
- **手動実行**: 任意（workflow_dispatch）
- **実行内容**: PR Ledger生成とPublic Mirror同期

### 3. Mirror Gate (Public Mirror)
- **ファイル**: `.github/workflows/mirror_gate.yml`
- **自動実行**: ✅ `main`ブランチへのpush時に自動実行
- **手動実行**: 任意（workflow_dispatch）
- **実行内容**: Public Mirrorへの同期

---

## 📋 手動実行が必要なワークフロー

### 1. 初回セットアップ時
- **Mirror Gate**: 最初の1回は手動実行推奨（動作確認のため）
- **SeventhSense**: 最初の1回は手動実行推奨（動作確認のため）

---

## 🔄 自動実行の確認方法

### 1. ワークフロー実行履歴の確認

```bash
# 最近の実行履歴を確認
gh run list --workflow=seventhsense.yml -R Seventh-Sense-Systems-S3/TriHexPhi --limit 5
gh run list --workflow=pr_ledger.yml -R Seventh-Sense-Systems-S3/TriHexPhi --limit 5
gh run list --workflow=mirror_gate.yml -R Seventh-Sense-Systems-S3/TriHexPhi --limit 5
```

### 2. 次の実行予定時刻の確認

GitHub Web UIで確認:
- リポジトリ → Actions → ワークフローを選択 → "Scheduled" タブ

---

## 💡 自動実行を確実にする方法

### 1. 初回手動実行（動作確認）

最初の1回だけ手動実行して、動作を確認:

```bash
# Mirror Gate（dry-run）
gh workflow run mirror_gate.yml -f dry_run=true -R Seventh-Sense-Systems-S3/TriHexPhi

# Mirror Gate（本番）
gh workflow run mirror_gate.yml -f dry_run=false -R Seventh-Sense-Systems-S3/TriHexPhi

# SeventhSense
gh workflow run seventhsense.yml -R Seventh-Sense-Systems-S3/TriHexPhi
```

### 2. 自動実行の確認

上記の手動実行が成功したら、以降は自動実行されます。

---

## 📝 外出中に完了確認する方法

### 1. GitHub Mobile App

- GitHub Mobile Appで通知を受け取る
- ワークフローの実行結果を確認

### 2. メール通知

- GitHub Settings → Notifications → Actions でメール通知を有効化

### 3. 次回帰宅後の確認

```bash
# 実行履歴を確認
gh run list -R Seventh-Sense-Systems-S3/TriHexPhi --limit 10

# 失敗したワークフローのみ確認
gh run list -R Seventh-Sense-Systems-S3/TriHexPhi --status failure --limit 10
```

---

## 🚫 手動実行を避けるための注意事項

### 1. ワークフローの重複実行を防ぐ

- **concurrency**設定が有効になっているか確認
- 同じワークフローが同時に実行されないように設定

### 2. 自動実行のスケジュール確認

- 毎日00:00 UTC（日本時間09:00）に自動実行される
- 手動実行は必要最小限に

### 3. 初回のみ手動実行

- 最初の1回だけ手動実行して動作確認
- 以降は自動実行に任せる

---

## ✅ チェックリスト

### 初回セットアップ時

- [ ] Mirror Gateを1回手動実行（dry-run）
- [ ] Mirror Gateを1回手動実行（本番）
- [ ] SeventhSenseを1回手動実行（動作確認）
- [ ] すべて成功したことを確認
- [ ] 以降は自動実行に任せる

### 日常運用時

- [ ] 手動実行は必要最小限に
- [ ] 自動実行のスケジュールを確認
- [ ] 実行履歴を定期的に確認

---

**生成者**: Cursor (AI Assistant)  
**目的**: ワークフロー自動実行ガイド  
**更新**: 2025-11-07

